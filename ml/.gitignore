# Python virtual env (never commit)
.venv/

# Python caches
__pycache__/
*.py[cod]

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# OS noise
.DS_Store

# Training data (CSV files are too large for version control)
data/

# Model artifacts (large binary files)
*.joblib
*.pkl

# Keep metrics/eval JSON files tracked (needed at runtime)
# Use: git add -f artifacts/metrics.json artifacts/eval_report.json

# Logs
*.log
